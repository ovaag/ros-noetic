jobs:
  stage_0_job_0:
    name: moveit-core base-local-planner navfn clear-costmap-recovery jsk-recognition-utils
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs: []
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-core ros-noetic-base-local-planner ros-noetic-navfn
        ros-noetic-clear-costmap-recovery ros-noetic-jsk-recognition-utils
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-core ros-noetic-base-local-planner ros-noetic-navfn
          ros-noetic-clear-costmap-recovery ros-noetic-jsk-recognition-utils
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_0_job_1:
    name: rtabmap-sync rtabmap-util image-transport-codecs rqt-rviz rtabmap-rviz-plugins
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs: []
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-rtabmap-sync ros-noetic-rtabmap-util ros-noetic-image-transport-codecs
        ros-noetic-rqt-rviz ros-noetic-rtabmap-rviz-plugins
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-rtabmap-sync ros-noetic-rtabmap-util ros-noetic-image-transport-codecs
          ros-noetic-rqt-rviz ros-noetic-rtabmap-rviz-plugins
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_0_job_2:
    name: librviz-tutorial mbf-costmap-core husky-description dbw-polaris-description
      dbw-mkz-description
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs: []
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-librviz-tutorial ros-noetic-mbf-costmap-core ros-noetic-husky-description
        ros-noetic-dbw-polaris-description ros-noetic-dbw-mkz-description
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-librviz-tutorial ros-noetic-mbf-costmap-core ros-noetic-husky-description
          ros-noetic-dbw-polaris-description ros-noetic-dbw-mkz-description
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_0_job_3:
    name: point-cloud-transport urdf-sim-tutorial perception move-slow-and-clear ros-ign
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs: []
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-point-cloud-transport ros-noetic-urdf-sim-tutorial ros-noetic-perception
        ros-noetic-move-slow-and-clear ros-noetic-ros-ign
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-point-cloud-transport ros-noetic-urdf-sim-tutorial
          ros-noetic-perception ros-noetic-move-slow-and-clear ros-noetic-ros-ign
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_0_job_4:
    name: imu-tools
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs: []
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-imu-tools
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-imu-tools
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_1_job_5:
    name: moveit-ros-occupancy-map-monitor rotate-recovery moveit-resources-prbt-ikfast-manipulator-plugin
      dwa-local-planner moveit-simple-controller-manager
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_0_job_0
    - stage_0_job_1
    - stage_0_job_2
    - stage_0_job_3
    - stage_0_job_4
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-ros-occupancy-map-monitor ros-noetic-rotate-recovery
        ros-noetic-moveit-resources-prbt-ikfast-manipulator-plugin ros-noetic-dwa-local-planner
        ros-noetic-moveit-simple-controller-manager
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-ros-occupancy-map-monitor ros-noetic-rotate-recovery
          ros-noetic-moveit-resources-prbt-ikfast-manipulator-plugin ros-noetic-dwa-local-planner
          ros-noetic-moveit-simple-controller-manager
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_1_job_6:
    name: chomp-motion-planner jsk-rviz-plugins rtabmap-viz rtabmap-slam rtabmap-odom
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_0_job_0
    - stage_0_job_1
    - stage_0_job_2
    - stage_0_job_3
    - stage_0_job_4
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-chomp-motion-planner ros-noetic-jsk-rviz-plugins ros-noetic-rtabmap-viz
        ros-noetic-rtabmap-slam ros-noetic-rtabmap-odom
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-chomp-motion-planner ros-noetic-jsk-rviz-plugins
          ros-noetic-rtabmap-viz ros-noetic-rtabmap-slam ros-noetic-rtabmap-odom
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_1_job_7:
    name: rqt-robot-plugins visualization-tutorials teb-local-planner husky-control
      global-planner
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_0_job_0
    - stage_0_job_1
    - stage_0_job_2
    - stage_0_job_3
    - stage_0_job_4
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-rqt-robot-plugins ros-noetic-visualization-tutorials
        ros-noetic-teb-local-planner ros-noetic-husky-control ros-noetic-global-planner
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-rqt-robot-plugins ros-noetic-visualization-tutorials
          ros-noetic-teb-local-planner ros-noetic-husky-control ros-noetic-global-planner
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_1_job_8:
    name: draco-point-cloud-transport carrot-planner husky-viz sensor-filters point-cloud-color
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_0_job_0
    - stage_0_job_1
    - stage_0_job_2
    - stage_0_job_3
    - stage_0_job_4
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-draco-point-cloud-transport ros-noetic-carrot-planner
        ros-noetic-husky-viz ros-noetic-sensor-filters ros-noetic-point-cloud-color
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-draco-point-cloud-transport ros-noetic-carrot-planner
          ros-noetic-husky-viz ros-noetic-sensor-filters ros-noetic-point-cloud-color
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_2_job_9:
    name: moveit-ros-planning move-base moveit-resources-prbt-moveit-config moveit-ros-control-interface
      jsk-interactive-marker
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_1_job_5
    - stage_1_job_6
    - stage_1_job_7
    - stage_1_job_8
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-ros-planning ros-noetic-move-base ros-noetic-moveit-resources-prbt-moveit-config
        ros-noetic-moveit-ros-control-interface ros-noetic-jsk-interactive-marker
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-ros-planning ros-noetic-move-base ros-noetic-moveit-resources-prbt-moveit-config
          ros-noetic-moveit-ros-control-interface ros-noetic-jsk-interactive-marker
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_2_job_10:
    name: rtabmap-launch viz simulators husky-gazebo point-cloud-transport-plugins
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_1_job_5
    - stage_1_job_6
    - stage_1_job_7
    - stage_1_job_8
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-rtabmap-launch ros-noetic-viz ros-noetic-simulators ros-noetic-husky-gazebo
        ros-noetic-point-cloud-transport-plugins
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-rtabmap-launch ros-noetic-viz ros-noetic-simulators
          ros-noetic-husky-gazebo ros-noetic-point-cloud-transport-plugins
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_2_job_11:
    name: husky-desktop
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_1_job_5
    - stage_1_job_6
    - stage_1_job_7
    - stage_1_job_8
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-husky-desktop
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-husky-desktop
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_3_job_12:
    name: moveit-kinematics moveit-ros-warehouse moveit-ros-robot-interaction moveit-ros-perception
      moveit-fake-controller-manager
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_2_job_9
    - stage_2_job_10
    - stage_2_job_11
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-kinematics ros-noetic-moveit-ros-warehouse ros-noetic-moveit-ros-robot-interaction
        ros-noetic-moveit-ros-perception ros-noetic-moveit-fake-controller-manager
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-kinematics ros-noetic-moveit-ros-warehouse
          ros-noetic-moveit-ros-robot-interaction ros-noetic-moveit-ros-perception
          ros-noetic-moveit-fake-controller-manager
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_3_job_13:
    name: moveit-resources-prbt-pg70-support moveit-planners-ompl turtlebot3-navigation
      husky-navigation jsk-interactive
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_2_job_9
    - stage_2_job_10
    - stage_2_job_11
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-resources-prbt-pg70-support ros-noetic-moveit-planners-ompl
        ros-noetic-turtlebot3-navigation ros-noetic-husky-navigation ros-noetic-jsk-interactive
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-resources-prbt-pg70-support ros-noetic-moveit-planners-ompl
          ros-noetic-turtlebot3-navigation ros-noetic-husky-navigation ros-noetic-jsk-interactive
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_3_job_14:
    name: desktop navigation moveit-visual-tools husky-simulator
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_2_job_9
    - stage_2_job_10
    - stage_2_job_11
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-desktop ros-noetic-navigation ros-noetic-moveit-visual-tools
        ros-noetic-husky-simulator
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-desktop ros-noetic-navigation ros-noetic-moveit-visual-tools
          ros-noetic-husky-simulator
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_4_job_15:
    name: moveit-ros-move-group moveit-plugins moveit-ros-benchmarks rtabmap-demos
      jsk-interactive-test
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_3_job_12
    - stage_3_job_13
    - stage_3_job_14
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-ros-move-group ros-noetic-moveit-plugins ros-noetic-moveit-ros-benchmarks
        ros-noetic-rtabmap-demos ros-noetic-jsk-interactive-test
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-ros-move-group ros-noetic-moveit-plugins
          ros-noetic-moveit-ros-benchmarks ros-noetic-rtabmap-demos ros-noetic-jsk-interactive-test
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_4_job_16:
    name: desktop-full turtlebot3 robot-body-filter
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_3_job_12
    - stage_3_job_13
    - stage_3_job_14
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-desktop-full ros-noetic-turtlebot3 ros-noetic-robot-body-filter
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-desktop-full ros-noetic-turtlebot3 ros-noetic-robot-body-filter
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_5_job_17:
    name: moveit-ros-manipulation rtabmap-legacy rtabmap-examples jsk-visualization
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_4_job_15
    - stage_4_job_16
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-ros-manipulation ros-noetic-rtabmap-legacy ros-noetic-rtabmap-examples
        ros-noetic-jsk-visualization
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-ros-manipulation ros-noetic-rtabmap-legacy
          ros-noetic-rtabmap-examples ros-noetic-jsk-visualization
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_6_job_18:
    name: moveit-ros-planning-interface rtabmap-ros
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_5_job_17
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-ros-planning-interface ros-noetic-rtabmap-ros
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-ros-planning-interface ros-noetic-rtabmap-ros
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_7_job_19:
    name: moveit-commander moveit-ros-visualization moveit-planners-chomp webots-ros
      moveit-servo
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_6_job_18
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-moveit-commander ros-noetic-moveit-ros-visualization
        ros-noetic-moveit-planners-chomp ros-noetic-webots-ros ros-noetic-moveit-servo
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-moveit-commander ros-noetic-moveit-ros-visualization
          ros-noetic-moveit-planners-chomp ros-noetic-webots-ros ros-noetic-moveit-servo
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_8_job_20:
    name: pilz-industrial-motion-planner-testutils moveit-setup-assistant moveit-ros
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_7_job_19
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-pilz-industrial-motion-planner-testutils ros-noetic-moveit-setup-assistant
        ros-noetic-moveit-ros
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-pilz-industrial-motion-planner-testutils ros-noetic-moveit-setup-assistant
          ros-noetic-moveit-ros
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
  stage_9_job_21:
    name: pilz-industrial-motion-planner panda-moveit-config moveit-planners moveit
    runs-on: macos-15-intel
    strategy:
      fail-fast: false
    needs:
    - stage_8_job_20
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Build ros-noetic-pilz-industrial-motion-planner ros-noetic-panda-moveit-config
        ros-noetic-moveit-planners ros-noetic-moveit
      env:
        ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_TOKEN }}
        CURRENT_RECIPES: ros-noetic-pilz-industrial-motion-planner ros-noetic-panda-moveit-config
          ros-noetic-moveit-planners ros-noetic-moveit
        BUILD_TARGET: osx-64
      run: |
        export CI=azure
        export GIT_BRANCH=$BUILD_SOURCEBRANCHNAME
        export FEEDSTOCK_NAME=$(basename ${BUILD_REPOSITORY_NAME})
        .scripts/build_unix.sh --target $BUILD_TARGET
name: build_osx64
on:
  push:
    branches:
    - buildbranch_osx
