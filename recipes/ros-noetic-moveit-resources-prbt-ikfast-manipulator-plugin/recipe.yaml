# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-noetic-moveit-resources-prbt-ikfast-manipulator-plugin
  version: 0.8.3
source:
  git: https://github.com/ros-gbp/moveit_resources-release.git
  tag: release/noetic/moveit_resources_prbt_ikfast_manipulator_plugin/0.8.3-1
  target_directory: ros-noetic-moveit-resources-prbt-ikfast-manipulator-plugin/src/work
  patches:
    - patch/ros-noetic-moveit-resources-prbt-ikfast-manipulator-plugin.patch

build:
  script: ${{ '$RECIPE_DIR/build_catkin.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_catkin.bat' }}
  number: 24
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://moveit.ros.org
  repository: https://github.com/ros-planning/moveit-resources
  license: Apache-2.0
  summary: The prbt_ikfast_manipulator_plugin package
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-catkin
    - ros-noetic-eigen-conversions
    - ros-noetic-moveit-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-tf2-eigen
    - ros-noetic-tf2-kdl
  run:
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-eigen-conversions
    - ros-noetic-moveit-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-tf2-kdl
