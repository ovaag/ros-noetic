# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-noetic-draco-point-cloud-transport
  version: 1.0.5
source:
  git: https://gitlab.fel.cvut.cz/cras/ros-release/point_cloud_transport_plugins.git
  tag: release/noetic/draco_point_cloud_transport/1.0.5-1
  target_directory: ros-noetic-draco-point-cloud-transport/src/work
  patches:
    - patch/ros-noetic-draco-point-cloud-transport.patch

build:
  script: ${{ '$RECIPE_DIR/build_catkin.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_catkin.bat' }}
  number: 24
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  repository: https://github.com/ctu-vras/draco_point_cloud_transport
  homepage: https://wiki.ros.org/draco_point_cloud_transport
  license: BSD-3-Clause
  summary: draco_point_cloud_transport provides a plugin to point_cloud_transport for sending point clouds encoded with KD tree compression.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-catkin
    - ros-noetic-cras-cpp-common
    - ros-noetic-draco
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-message-generation
    - ros-noetic-pluginlib
    - ros-noetic-point-cloud-transport
    - ros-noetic-roslint
    - ros-noetic-sensor-msgs
    - ros-noetic-std-msgs
  run:
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-cras-cpp-common
    - ros-noetic-draco
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-message-runtime
    - ros-noetic-pluginlib
    - ros-noetic-point-cloud-transport
    - ros-noetic-sensor-msgs
    - ros-noetic-std-msgs
