# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-noetic-hector-gazebo-plugins
  version: 0.5.4
source:
  git: https://github.com/tu-darmstadt-ros-pkg-gbp/hector_gazebo-release.git
  tag: release/noetic/hector_gazebo_plugins/0.5.4-1
  target_directory: ros-noetic-hector-gazebo-plugins/src/work

build:
  script: ${{ '$RECIPE_DIR/build_catkin.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_catkin.bat' }}
  number: 24
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/hector_gazebo_plugins
  license: BSD-3-Clause
  summary: hector_gazebo_plugins provides gazebo plugins from Team Hector. Currently it contains a 6wd differential drive plugin, an IMU sensor plugin, an earth magnetic field sensor plugin, a GPS sensor plugin and a sonar ranger plugin.
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-catkin
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-gazebo-dev
    - ros-noetic-geographic-msgs
    - ros-noetic-geometry-msgs
    - ros-noetic-message-generation
    - ros-noetic-nav-msgs
    - ros-noetic-roscpp
    - ros-noetic-std-msgs
    - ros-noetic-std-srvs
    - ros-noetic-tf
  run:
    - gazebo
    - libabseil
    - libprotobuf
    - python
    - ros-distro-mutex 0.7.* noetic_*
    - ros-noetic-dynamic-reconfigure
    - ros-noetic-gazebo-ros
    - ros-noetic-geographic-msgs
    - ros-noetic-geometry-msgs
    - ros-noetic-message-runtime
    - ros-noetic-nav-msgs
    - ros-noetic-roscpp
    - ros-noetic-std-msgs
    - ros-noetic-std-srvs
    - ros-noetic-tf
